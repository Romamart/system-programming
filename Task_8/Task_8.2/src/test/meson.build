gtest = dependency('gtest', main:true)
test('sort',executable('TableTest',
                sources: ['TableTest.cpp'],
                include_directories: src,
               dependencies: [gtest]
    )
    
)
