gtest = dependency('gtest', main:true)
test('sort',executable('sort_test',
                sources: ['sort_test.cpp'],
                include_directories: src,
               dependencies: [gtest]
    )
    
)
